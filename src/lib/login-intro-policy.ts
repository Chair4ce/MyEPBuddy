/**
 * Optional stacked login intros (coaching, trial, earn-tokens, welcome tour,
 * EPB prompt-update modal, Impact Booster "New" highlight) no longer auto-open.
 * Unfinished terms still block. The purchase-campaign promo still shows once
 * while its window is open. After the window ends those feature intros stay off.
 */
export const AUTO_FEATURE_LOGIN_INTROS_ENABLED = false;
