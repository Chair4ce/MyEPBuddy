# Implementation Plans

Generated by the improve skill on 2026-07-21 (auth signup/login working-tree audit at commit `7ca7205`, plus uncommitted auth page edits). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Stabilize restricted-browser `useSyncExternalStore` snapshot on signup | P1 | S | — | DONE |
| 002  | Share restricted-browser helper and drop login `useEffect` for UA detection | P2 | S | 001 | DONE |
| 003  | Add auth characterization tests (errors + restricted-browser) | P2 | M | 001 | DONE |
| 004  | Remove dead phone-signup affordances (copy + CTAs only) | P2 | S | 003 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED | REJECTED

## Dependency notes

- **001 before 002**: 002 consumes the module created in 001; do not duplicate detectors.
- **001 before 003**: tests target the pure helpers introduced in 001.
- **003 before 004**: tests lock `shouldCreateUser: false` / `otp_disabled` messaging before UX copy changes in 004.
- **004 last (deferred)**: only plan that changes user-visible signup affordances (removes Phone-as-signup). Plans 001–003 are bugfix / extract / tests with no auth-method removal. **Do not** enable phone account creation in 004 — that is a separate product/security decision (SMS confirmations are off in `supabase/config.toml`).

## Breaking-change posture

| Plan | User-visible auth behavior change? | Notes |
|------|--------------------------------------|-------|
| 001 | No | Fixes potential infinite re-render; same Google-in-webview warning |
| 002 | No | Same detection rules; login toast `useEffect` for query params stays |
| 003 | No | Tests only |
| 004 | Yes (signup CTA / copy) | Deferred to end. Phone **sign-in** on `/login` and `/phone-login` must keep working. True phone **signup** (`shouldCreateUser: true`) is explicitly out of scope |

## Findings considered and rejected (this pass)

- Splitting giant login/signup page components — low leverage vs 001–004.
- Syncing production Resend/Dashboard magic-link subject (`Your Magic Link` vs repo `Sign in to MyEPBuddy`) — ops/Dashboard, not this working tree.
- Enabling phone OTP account creation — security risk while `[auth.sms] enable_confirmations = false`; needs a separate product plan.

## Prior dated plans (unrelated)

These predate the numbered improve series; leave them as historical:

- `2026-07-09-fix-all-supabase-log-errors.md` (DONE)
- `2026-07-09-fix-team-members-token-reward-trigger.md` (DONE)
